/**
 * Default constants aligned with antdv-next.
 * Single source of truth — all defaults reference these values.
 */

/** Default Emotion cache key for CSS class generation. */
export const DEFAULT_CSS_PREFIX_KEY = 'antdv-css'

/** Default component CSS class prefix, matching antdv-next's defaultPrefixCls. */
export const DEFAULT_PREFIX_CLS = 'ant'

/** Default icon CSS class prefix, matching antdv-next's defaultIconPrefixCls. */
export const DEFAULT_ICON_PREFIX_CLS = 'anticon'

/** Default CSS variable prefix, matching antdv-next's cssVar.prefix. */
export const DEFAULT_CSS_VAR_PREFIX = 'ant'
